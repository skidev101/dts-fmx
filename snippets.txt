{/* Courses */}
          {/* <Controller
              name="course"
              control={form.control}
              render={({ field, fieldState }) => (
                <Field data-invalid={fieldState.invalid}>
                  <FieldLabel>Courses</FieldLabel>
                  <Select value={field.value} onValueChange={field.onChange}>
                    <SelectTrigger>
                      <SelectValue placeholder="Select level" />
                    </SelectTrigger>
                    <SelectContent>
                      {courses?.map((lvl: string) => (
                        <SelectItem key={lvl} value={lvl.toString()}>
                          Level {lvl}
                        </SelectItem>
                      ))}
                    </SelectContent>
                  </Select>
                  {fieldState.invalid && (
                    <FieldError errors={[fieldState.error]} />
                  )}
                </Field>
              )}
            /> */}

          {/* Course */}
          {/* <Controller
              name="courseId"
              control={form.control}
              render={({ field, fieldState }) => (
                <Field data-invalid={fieldState.invalid}>
                  <FieldLabel>Course</FieldLabel>
                  <Select value={field.value} onValueChange={field.onChange}>
                    <SelectTrigger>
                      <SelectValue placeholder="Select course" />
                    </SelectTrigger>
                    <SelectContent>
                      {courses.map((course) => (
                        <SelectItem key={course.id} value={course.id}>
                          {course.name}
                        </SelectItem>
                      ))}
                    </SelectContent>
                  </Select>
                  {fieldState.invalid && (
                    <FieldError errors={[fieldState.error]} />
                  )}
                </Field>
              )}
            /> */}

          {/* Tags (Combobox) */}
          {/* <Controller
              name="tags"
              control={form.control}
              render={({ field }) => {
                const handleSelect = (tag: string) => {
                  const newTags = [...(field.value || []), tag];
                  field.onChange(newTags);
                  setSelectedTags(newTags);
                };
                const handleRemove = (tag: string) => {
                  const newTags = (field.value || []).filter((t) => t !== tag);
                  field.onChange(newTags);
                  setSelectedTags(newTags);
                };
                return (
                  <Field>
                    <FieldLabel>Tags</FieldLabel>
                    <div className="flex flex-wrap gap-2 mb-2">
                      {selectedTags.map((tag) => (
                        <span
                          key={tag}
                          className="px-2 py-1 bg-gray-200 rounded flex items-center gap-1"
                        >
                          {tag}
                          <button
                            type="button"
                            onClick={() => handleRemove(tag)}
                          >
                            ×
                          </button>
                        </span>
                      ))}
                    </div>
                    <Popover>
                      <PopoverTrigger asChild>
                        <Input placeholder="Search tags..." readOnly />
                      </PopoverTrigger>
                      <PopoverContent className="w-[300px] p-0">
                        <Command>
                          <CommandInput placeholder="Type to search tags..." />
                          <CommandList>
                            <CommandEmpty>No tags found</CommandEmpty>
                            {availableTags
                              .filter((tag) => !selectedTags.includes(tag))
                              .map((tag) => (
                                <CommandItem
                                  key={tag}
                                  onSelect={() => handleSelect(tag)}
                                >
                                  {tag}
                                </CommandItem>
                              ))}
                          </CommandList>
                        </Command>
                      </PopoverContent>
                    </Popover>
                  </Field>
                );
              }}
            /> */}